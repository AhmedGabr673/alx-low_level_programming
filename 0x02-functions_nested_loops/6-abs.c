#include "main.h"

/**
 * _islower - checks for lowercase character
 * @x: int to check
 * Return: 1 if lowercase, 0 if uppercase
 */

int _abs(int n)
{
	if (n > 0)
		return (n);
	else if (n < 0)
		return (-n);

	return (0);
}

