#include "main.h"

/**
 * puts2 - print every other char of a string
 * @str: string to reverse
 */

void puts2(char *str)
{

while (*s != '\0')
	{
		len++;
		s+=2;
        _putchar(*s);
	}
     _putchar('\n');
}
